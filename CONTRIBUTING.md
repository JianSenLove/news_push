# 🛠️ 团队协作与开发规范 (Contributing Guidelines)

欢迎参与本项目！为了保证多人协作的代码质量和项目秩序，请所有参与开发的成员务必遵守以下开发约定：

## 1. 📝 功能记录规范
开发功能（包括新特性与优化项）需要统一在 [OPTIONS.md](./OPTIONS.md) 文件中进行记录和登记。大家先认领，再开发，避免撞车和重复造轮子。

## 2. 🌿 分支管理策略
由于有多人共同维护项目，在开发**新功能点**或者处理**Bug 修复**时，**严禁在主分支直接提交代码**，必须新开独立的 `feature` 分支（或 `bugfix` 分支）。
* 命名推荐：`feature/your-feature-name` 或 `bugfix/issue-description`

## 3. 🧪 质量保证与合并原则
项目崇尚 TDD（测试驱动开发），**功能点开发完成后，必须进行本地的回归测试**，确认无误、达到质量要求后，再通过 Pull Request 的形式合并到 `master` 主分支。

## 4. 🔀 非功能点直接提交
对于**非功能点**的开发或修改（比如：README 文档补充、拼写错误修复等），允许直接向主分支 (`master`) 提交和 Push。

## 5. 📋 任务分配与追踪
开发独立的功能点时，需要提交相应的 **TASK** 以及对应的 **PLAN**（可以利用代码托管平台的 Issue / Milestone 或是单独的日志文件）。这样以便其他成员能够了解进度，并在必要时接手继续跟踪开发过程。

---
> 🌟 所有的修改和 PR，都会在 Code Review 时检查是否符合以上约定。感谢你为 `news_push` 项目做出的贡献！
