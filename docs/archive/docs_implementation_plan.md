# 每日科技前沿赛博冲浪助手 - 实施方案 (Implementation Plan)

本文档基于 TDD (测试驱动开发) 的原则，详细阐述“赛博冲浪助手”项目的系统架构、实施策略及技术细节。

## 1. 核心目标
构建一个能够模拟“赛博冲浪者”的自动化应用。该应用将每天定时从指定的技术资讯源获取信息，使用大模型提炼和总结高价值内容，**优先在本地生成排版优美的 Markdown 资讯日报**，并**作为可选功能**将其推送到第三方聊天软件（如飞书）。

**运行环境:**
- **OS**: Windows
- **Language**: Python 3.14.2
- **工作目录**: `d:\project\news_push`

## 2. 架构设计与开发原则

项目将严格遵循 **TDD (Red-Green-Refactor)** 模式。所有的功能开发必须先编写单元测试用例，并在测试不通过的情况下编写功能代码直到绿灯。
为了能够在上下文中安全地保持进度，项目强制使用 `task.md` 追踪各个细碎的任务项，并使用 `progress_log.md` 作为开发历史日志。

## 3. 核心模块与拆分策略 (模块解耦)

### 阶段 0: 脚手架与环境初始化 (Scaffolding First)
- **目标**: 搭建空壳项目骨架。**完全不涉及任何业务逻辑。**
- **产出**: 
  - `requirements.txt`: 引入开发级依赖 `pytest`。
  - 基本目录结构 (`src/`, `tests/`, `config/`)。
  - `.env.example`: 系统级配置模板。
  - 第一条空测试用例 `tests/test_env.py`，证明框架挂载成功。

### 阶段 1: 资讯探测层 (Ingestion)
- **目标**: 从前沿信源爬取并解析数据。
- **职责**: 解析来源（如标准 RSS 订阅，或适配 RSSHub 转换的特殊 API）。提取抽象出的标准新闻实体：`Article(id, title, summary, link, source)`。
- **TDD策略**: Mock 本地静态的 RSS/XML 文件，进行解析测试，不依赖外部网络环境。

### 阶段 2: 防重壁垒 (Storage)
- **目标**: 防止昨天推送过的新闻今天被再次推送（避免信息茧房和噪音）。
- **职责**: 使用轻量级的本地存储（`SQLite` 或 `JSON`），基于文章链接或其哈希值记录处理状态。
- **TDD策略**: 编写测试在本地虚构存储中插入和检查 Article ID。

### 阶段 3: 赛博编辑大脑 (AI Processing)
- **目标**: 清洗杂音，提取关键价值点，自动翻译与总结“极客式”导报。
- **职责**: 建立明确的系统人格（Persona Prompt），向大模型（如 DeepSeek/OpenAI）下发经过聚合的新闻摘要，返回结构化/分红分类的结果。
- **TDD策略**: 完全 Mock 掉远端 LLM 网络调用，只针对函数在收到假设字典后的表现及其容错处理编写用例。

### 阶段 4: 出版社生成引擎 (Local Delivery)
- **目标**: 优先跑通本地闭环，确保资料留存并可人工校验。
- **职责**: 将 AI 处理后结构化的信息，格式化为一个完整的、排版优美的 `YYYY-MM-DD-daily-news.md`。包含适当的任务分割线、列表、加粗以及原始链接。
- **TDD策略**: 将 Mock 出来的“模型总结字典”扔给排版生成器，断言是否成功在本地 `ouput/` 目录写出预期的文件内容和格式。

### 阶段 5: 快递员推送模块 (Feishu Optional Delivery)
- **目标**: 支持外部平台的接入（独立开发，不影响主流程）。
- **职责**: 读取本地的 Markdown 文件内容，使用飞书设定的 Webhook 转化为飞书支持的富文本卡片格式并使用 POST 请求抛出。
- **TDD策略**: 依赖上一阶段的本地文档内容，Mock 飞书机器人的地址和网络请求代码，验证是否能合法并成功地发起 payload。

## 4. 自动化流水 (Pipeline Run)
主程序 `main.py` 负责把所有的模块（Ingestion -> Storage -> Processing -> Local Delivery -> Remote Delivery）像积木一样拼装，捕捉任何层间的异常，并可通过比如 Windows 自带任务计划程序等工具实现每天指定时间（例如 08:00 AM）准时运行。

## 5. 渠道规范化与高可用校验 (Source Validation - 追加需求)
- **目标**: 剥离代码中硬编码的 RSS 源，实现灵活配置与提前校验。
- **职责**:
  - 创建独立的 `config/sources.json` 维护信源池。
  - 构建 `SourceManager` 类（位于 `src/ingestion/`），负责读取配置，并提供一个可用性校验机制（如超时检测、HTTP 状态码检测），能够动态剔除不可用源。
- **TDD策略**: 增加针对配置读取和校验存活状态机制的单元测试。

## 6. 频道分类分类架构设计 (Category Management)
- **目标**: 支持多种不同主题的资讯源归类（如 AI科技、国内时政、泛娱乐）。
- **职责**:
  - 改进配置为字典嵌套数组结构：`{"categories": {"AI科技": [...], "数字生活": [...]}}`。
  - `SourceManager` 支持结构化拉取配置信息。
  - 保持底层大模型处理环节 (`AIProcessor`) 的结构化数据输出能力。能够为不同分类下发独立提炼指令。

## 7. 轻量级驱动引擎 API 化 (Backend API Engineering)
- **目标**: 将现有的 Python 单机处理流水线改造为标准的 Web 后端微服务，方便前端拉取数据。
- **职责**:
  - 引入高性能异步框架 `FastAPI` 及 `uvicorn`。
  - 构建 `/api/categories` 接口：直接解析更新后的 `sources.json` 抛出顶层菜单。
  - 构建 `/api/news` 接口：接收前端传来的分类参数（如 `?category=AI科技`），并在后台按需触发 `SourceManager -> 抓取 -> 去重 -> LLM 处理` 流水线，最后将 AI 返回的数据直接透传给前端，而不是留在本地生成 Markdown。
- **TDD策略**: 使用 `TestClient` (由 fastapi 提供)，撰写对应的路由可用性断言测试。

## 8. 专属 Web 冲浪面板开发 (Frontend UI Browser)
- **目标**: 构建具有极客美学、现代感的专属读报交互应用程序客户端。
- **职责**:
  - 在 `src/web/` 目录下开发采用原生技术栈 (`HTML` + `Vanilla CSS` + `JS`) 兼顾极高轻量级的单页面应用 (SPA)。
  - **极致视觉体验**: 提供具有现代感甚至赛博质感（例如深色暗夜模式、Glassmorphism 毛玻璃特效、平滑过渡动画）。
  - **交互体验**:
    - **顶部/侧边导航**: 动态从 `/api/categories` 渲染出所有支持的新闻板块，构建 Tab 栏。
    - **内容视窗**: 每次点击栏目后，展现顺滑的 Loading 骨架屏动画，防止白屏死等。
    - **瀑布流卡片**: 根据后端下发的数据进行流式或卡片式的优美渲染。
  - **运行模式**: 提供 `run_server.bat`，一键启动后端服务供本地 `http://localhost:8000` 浏览。

## 9. 阶段 13: 静态化解耦渲染架构 (Static Data Decoupling)
- **目标**: 解决 Web 端“即时抓取+LLM分析”带来的高延迟与重复消耗资源问题，改为直接展示已有（`main.py` 每日定时产出）的分析结果，实现零延迟、点击即看的纯享体验，并额外提供手动重刷按钮。
- **职责**:
  - `main.py` 双写机制：原有的生成 `YYYY-MM-DD-dailynews.md` 逻辑不变作为文档留存。新增 JSON 固化逻辑，将跑出的 `ai_summary_json` 最终合并作为一个完整对象树写入 `output/latest_news.json`。
  - `server.py` 接口改造：
    - 新增 `/api/news/latest`：毫秒级响应，直接读取本地预先生成的全量分类快照 `latest_news.json`。
    - 移除或停用原来的 `/api/news/generate` 实时大轮询路由。
    - 新增 `/api/news/refresh`（可选实现）：后台一键唤起 `main.py` 流水线，并抛出执行状态供前端。
  - 前端重整 (`app.js`)：初始加载时请求 `latest` 接口；所有的 Tab 切换只操作本地内存视图，真正“零迟滞”。增加独立【全量更新】按钮触发重新制备大盘快照。
- **TDD策略**: 更新 API 测试用例确保 `/api/news/latest` 能够正确读取底层文件并被代理给前端。
